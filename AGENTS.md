# Big-Chicken Houdini Studio development

**Current blocker (2026-09-11):** The owner authorized the [R4-HELP-1 review](docs/r4-help-review.md). Studio-launched Houdini help is blank; the same native installation is reported normal. Root cause and installed-payload GUI impact remain unknown. Pause automatic Ready/merge/Public RC for PR #14. First audit a strictly read-only, main-thread inspection of the actual help pane, then obtain matching real GUI evidence when the owner is available. No global Qt object scans, raw-pointer wrapping, lazy engine creation, speculative PATH/DLL changes or further invalid offscreen/hython browser probes. Preserve the prior failed probes. Only fix the boundary proven responsible, preserving native preferences, the six Houdini search paths, workspace cwd/history and Clash. Production/package changes require a new candidate and actual payload GUI verification. See [issue status](docs/acceptance-issues.md) and [bounded procedure](docs/r4-help-validation.md).

**Owner environment correction (2026-09-11):** The owner authorized the reviewed custom Houdini search-path fix, commit and new ZIP. Normal Launcher/host startup carries the explicit search-variable list in `launcher_environment()` and appends Studio's package directory; ordinary helpers and isolated tests stay sanitized. Preserve Python/Qt/HFS cleanup, user files, native preferences and all execution/permission boundaries. See `docs/houdini-environment-validation.md`; headless HDA discovery/cook evidence does not replace standard-user GUI acceptance.

The owner also reported generated VEX landing in the native workspace. Generated source exports are scene outputs, including inspection exports: use the existing `output_path()` policy, defaulting saved scenes to `$HIP/BigChickenStudio/<HIP stem>/{renders,exports,assets}`. Only disposable internal scratch belongs in the private workspace. Preserve explicit/existing destinations and old files/cwd/history; do not add write interception or broaden permissions.

**Owner artwork correction (2026-09-10):** The owner explicitly requested the supplied Downloads illustration as the static Launcher background, a youthful white/blue palette, readable translucent nodes and the girl portrait as the Studio.exe icon. This overrides earlier Launcher pink/dark and no-brand-art restrictions. Use redesign-existing-projects only to improve this approved presentation. Keep the five-node topology, Changa hero, Lucide functional icons, native Qt, Panel presentation and execution architecture. Resources are bundled under src/studio/ui/assets/launcher-artwork; no runtime download.


This is a new repository. The old HIA checkout is reference material, never a write target.
The latest approved Launcher scope is `docs/launcher-node-flow-brief.md`: fixed Account -> Empty/Open/Recent branches -> Launch, native QWidget layouts and geometric connections, Changa One Italic hero, the owner-approved local white/blue palette and static supplied artwork. Selection never starts; only the Launch sink enters existing admission. This supersedes earlier Launcher page/activation/hero restrictions, not Panel or execution architecture. Preserve single Studio.exe, Owner development location, native user Houdini preferences and explicit test isolation. New resources/UI require a new candidate, native previews, CI and a five-file ZIP; Owner GUI/preferences and external standard-user acceptance remain separate real gates. The subsequently authorized cleanup is complete: only the latest ZIP and checksum remain in the ignored 发布包 directory; durable data and changed historical worktrees remain preserved. The README-1 outer handoff documents are versioned in release/handoff and do not change installer bytes. See docs/launcher-node-flow-validation.md for current hashes and cleanup evidence.
The user's 2026-09-10 delivery correction authorizes a single `Studio.exe` entry for development and installed use; Start Menu points to that EXE and Python bootstrap stays internal. This packaging-only change requires a new candidate, preserving the f0fce6e baseline. This turn prepares files and the ZIP only: no Owner installation, external test, UI refinement or network fix. Preserve the Owner's selected project and data locations.
The current limited follow-up is `docs/rc-trial-scope.md` with the full Pro audit at `docs/rc-trial-review.md`. It permits Owner daily RC use and named-tester distribution, shared Houdini Validated/Untested/Unsupported/Unknown policy plus explicit trial confirmation, and missing read-only identity/provenance facts. These bounded product changes require a new candidate; preserve `991cbce` as historical rollback/evidence. The user's direct Clash correction overrides the audit: Clash remains required for external services, each user uses their own configured port, and no Clash-off/no-Clash environment test is required. Never hardcode the developer's port or change global proxy settings. Internal loopback remains direct. Do not reopen NET-1, steer, tools, prompts, consent or execution architecture. PR #14 remains Draft until external standard-user acceptance and final Pro Go.
The current delivery candidate is `0.1.0-rc.1-8af563981e73`, source/builder `8af563981e73995825f0e35bd6e1d11dd74f7c64`. It adds the user-authorized Houdini search-path correction, generated-scene-output instructions and previously committed secondary-page/background/caption/taskbar fixes. Five source CI jobs, 24 environment/storage/launcher tests, four output-policy tests, real headless H22 HDA comparison, 244 manifest checks and five-file ZIP readback passed. The actual packaged EXE opened and closed with exit code 0. See `docs/houdini-environment-validation.md`; prior candidate `042c66b9be95` and its close observations remain historical. Standard-user installation/authoring and fresh-model output-policy compliance remain untested. No Owner installation or external test was performed. Further product/package edits require a new candidate.
The architectural acceptance briefs are `docs/pro-diagnosis.md` and `docs/stage-readiness-review.md`.
The latest scope and acceptance decision is `docs/final-release-brief.md`, supplementing `docs/release-readiness-brief.md`. Continue draft PR #14 on `codex/windows-release-package`: stabilize R4-NET-1 first, implement native same-Turn steering second, rebuild the final RC, then complete the Windows 11 x64 standard-user installed workflow before Ready/merge/public preview. These are the three release gates; no other capability, benchmark or visual expansion is authorized. PR #10 technically merged at `890284b`; model-quality gaps remain in `docs/model-acceptance-results.md`. R1/#11, R2/#12 and R3/#13 merged at `fe57c7a`, `2b180da` and `b48ceae`; their historical evidence remains versioned. PR #8/#9 consent/conversation boundaries and PR #5 closure remain. The user has no suitable standard-user test environment yet; freeze the final candidate if this last gate remains unavailable rather than filling the wait with more features.
Read `docs/rebuild-brief.md` before changing a subsystem. Do not replace this effort with a generic agent platform.

R4 integrated package `3150c15` reproduced intermittent native reply/history failure R4-NET-1. The local HTTP IO correction at `a5731cf` passed its focused fault tests, CI and two fresh actual-package Houdini GUI checks; see `docs/r4-net-validation.md`. Preserve the original failures and the identified native reply alias evidence. R4 stays draft until native steer and the final installed standard-user workflow pass. Do not replace Houdini Qt or invent a UI/icon redesign.

PANEL-STEER-1 is implemented and passed fixed-binary, fault, recovery and real H22 same-Turn authoring checks in frozen candidate `991cbce`; see `docs/r4-final-candidate.md`. Its final-package NET regression, CI, diagnostics and fresh HIP reload also passed. Only the standard-user installed workflow remains untested; keep PR #14 Draft and do not rebuild or expand features while waiting. Preserve pinned Codex 0.153.4 `turn/steer`, immutable click-time start/steer intent, exact client message identity, one unresolved user send per Thread, original-account recovery, bounded late results and no replay. ACK settles input acceptance, never revives a completed/stopped Turn or Runtime owner. Send and Stop coexist using existing approved icons. Steer preserves approval, consent, model/effort/cwd, scene epoch and accepted staged payloads. Dynamic Artwork is post-release direction only; no assets, shaders or dependencies now.

- Resolve the installation root from this checkout or HIA_PROJECT_ROOT. Installation resources and dependencies remain under it. Normal user state and disposable cache use separate platform roots, resolved by the launcher and passed explicitly to children. Preserve containment for each root. Development fixtures, previews and logs stay in this checkout's .runtime; tests must never use real user state.
- Existing .runtime workspaces, native history, authentication and referenced artifacts are durable legacy data. Access them explicitly in place; do not automatically move, delete, renumber or rewrite them. Saving or renaming a HIP does not move its active workspace, ledger or native cwd.
- Preserve all user data and unrelated changes. Do not reset, clean, delete, move or overwrite user files. Use short-lived branches and commit only owned files; preserve public main history.
- Never modify the Houdini installation or rewrite/copy user configuration. Per the user's explicit 2026-09-10 correction, normal Studio launch uses Houdini's existing user preferences (including an existing HOUDINI_USER_PREF_DIR override), preserving layouts, colors and shortcuts. Only tests use isolated project-local Houdini preferences; helper children carry the selected mode. Temporary directories and Studio integration remain child-only.
- All local services bind 127.0.0.1 and use a fresh launcher-session token. Do not put tokens in files, command lines, results or logs.
- Codex is the only reasoning/content system. Keep native Codex Thread/Turn history and automatic compaction. No second agent, planner, recovery prompt generator, automatic memory or summarizer.
- The runtime owns scene identity and operation receipts. Validate observed scene_epoch inside the main-thread execution callback. Never silently refresh an observation before a write.
- Persist receipts before responding. Re-query operation_id; never replay scripts to recover a missing response. General HOM external effects are unknown and automatic_retry_safe is false even after Undo.
- All scene reads/cooks/captures/writes share one bounded queue. Health reads cached state without HOM. Codex Stop and Houdini execution are distinct facts.
- Keep checks targeted and optional. No mandatory lookup, whole-scene snapshot, screenshot or repeated validation for deterministic edits.
- Knowledge import/FTS and explicit workspace decisions are independent of live Houdini and never block startup. Do not index plugin-development documents as default scene knowledge. No embedding dependency in the core release.
- Panel and launcher must be substantially new interactions and layouts, not a skin over the old UI. Panel projects facts; it does not infer HOM completion from Codex events.
- Launcher and Panel follow docs/ui-presentation-brief.md and the user's explicit corrections. Product icons use only the approved Lucide Outline 0.468.0 SVG subset at f12b0de177fbc2a6795e99be065887e72b237123, with original geometry and bundled license notices. No custom icons, graphic logo, emoji or QStyle.SP_* product fallback; missing icons fall back to text. Native OS dialogs and title bars keep their system icons. Do not add unapproved controls, a legacy-context browser or profile-switching UI.
- The completed PR #5 presentation correction did not reopen storage, native protocol or permission policy. Page transitions project existing facts and cannot create a probe, login or launch. Its real workflow acceptance remains versioned in `docs/authoring-results.md`.
- TC-3 supports 2–8 linear HOM steps within one existing operation and queue entry. Validate all scripts before mutation; separately dispatch each step, persist start/result before advancing, pass bounded complete JSON, and halt on failed gates, cancel, context change or unknown persistence. No replay/resume execution API, second queue/worker, workflow database, hidden Python session or render expansion. Real H22 GUI step boundaries, Stop and targeted correction are technical merge gates. Preserve independent frozen TC1-A1/TC2-A1 experiments; any TC3-A1 model evidence is separate.
- Preserve prompt policy, precise versioned approval correlation, trust revocation, workspace/Thread ownership and native history authority. Only confirmed native delete may clear that Thread's draft; never delete shared workspace receipts/artifacts.
- R2 must not impose call quotas, mandatory discovery/check/capture/staged/observe_after rules or a narrower HOM language. Script readback is a valid alternative. Metadata caches are request-local; preserve lifecycle filters, sorting, real cursor positions and full original detail. No persistent index or relaxed receipt/step-gate durability. Frozen model results stay separate from runtime measurements; do not rerun them to manufacture a pass.
- R1 authorizes a small in-memory projection reducer keyed by connection generation, Thread, Turn and Item. Fence history/image/layout callbacks; terminal items outrank stale or partial history. Never deduplicate by text/length or guess snapshot/delta alignment. Verify pinned native pagination against the actual store and remember unsupported capability per connection. Coalesce drawing only, not data reception; keep full source readable/copyable. No second chat database, prompt changes or theme redesign. Preserve original PANEL-1 and obtain old-failing/new-passing tests plus real Houdini Panel evidence before closure. PANEL-1 blocks public release.
- An untested cross-monitor DPI matrix alone is not a merge blocker; observed wrong clicks, an off-screen model popup or an inaccessible Stop control are blockers. Do not mark untested behavior passed.
- Do not use redesign-existing-projects as a default step. For a confirmed local UI blocker, read the latest closure brief and the approved presentation specification first; any Skill advice remains subordinate to them. Keep the page structure, font system, pink direction, approved icons and PySide6 architecture.
- Do not control Houdini with Computer Use. Native Qt offscreen UI review is allowed for development, and does not constitute a real Houdini GUI test.
- Use a small meaningful fault-test set, one static check and focused UI checks. Do not repeatedly run full test suites, compute arbitrary hashes or create process-heavy verification scaffolding. Report what is actually verified and what remains unverified.
