# Deferred model acceptance

Technical integration and proven model benefit are separate under the
[latest Pro approval](tc2-brief.md). Pending experiments remain owned and visible.

## TC1-A1 — two-turn railing comparison

- **Owner:** Codex. **Status:** running with explicit user authorization. The
  baseline's two turns completed; the frozen candidate arm is running. Full
  behavior/quality review is pending, not passed.
- **Baseline:** `1e9f0f4dc43465bd221b3e0832a5e75d6cd1aa66`.
- **Frozen candidate:** immutable Git tag `tc1-a1-candidate`, commit
  `ecef5b3f2dae6e95ccd6feb12507a0761f646d10`, also registered in PR #6 before its
  technical merge at `c409c88be6c4b75a9cf4c5167a78187bf2812462`. Never move this tag. Use this
  candidate, not the future TC-2 branch/main tip, for the candidate arm.
- **Fixture:** `.runtime/reviews/tc1/fixture/source.hip`, generated by
  `scripts/tc1_fixture.py` at `835e05b`; initial inventory is `source-report.json`.
  Preserve the existing source in place and run each arm on a separate copy. No
  prebuilt railing. Fresh-load/cook check: 21 nodes, zero errors, recorded separately
  from model acceptance. Keep the original input and every failed output.
- **Houdini:** 22.0.368 for both arms. **Codex executable:** the same installed
  native `codex-cli 0.153.4` binary for both arms, version rechecked before running.
- **Model/effort:** `gpt-6-astra` / `high`, identical for both arms. The authenticated
  native catalog confirms both; baseline native turn_context records also confirm
  actual model/effort. Retain corresponding candidate native records. If unavailable
  or rerouted, stop comparison admission and record the condition; do not silently
  switch one arm or claim matched conditions.
- **Authorization update:** the user explicitly authorized all three model
  experiments and scene-operation permission in their new conversations. The TC2
  isolated acceptance account is signed in. Both arms use that test state and
  CODEX_HOME explicitly in place; credentials/history are never copied or reset.
  New workspaces/receipts/native history stay under `tc2/acceptance/state`, while
  scene copies, exports and reports stay under `tc1/model/<arm>-<run_id>`.
- **Recovery:** `.runtime/reviews/tc1/Start TC1 Acceptance.vbs` is the ordinary
  launcher with dedicated `acceptance/state` and `acceptance/cache`. Full operator
  steps are in [tc1-validation.md](tc1-validation.md). Prior local observations,
  backend checks and complete native exports/images are recorded under the explicit
  review roots above. The old TC1-only launcher remains available but is not the
  authenticated state root of these authorized driver runs.
- **Priority:** execute at the next available authorized model-run opportunity.
  TC-2 review must report this record's status. Keep it pending if login remains
  unavailable; do not remove it or substitute TC-2 task evidence.

Run independent fresh native Threads for baseline and candidate, each with two
turns on the same working HIP. The model sees only normal Studio and the following
prompts, not the briefs, expected nodes, call counts or backend probe results.

### Frozen first prompt

> 请利用场景里已有的曲线、坡面和两种立柱模块，沿步道外侧做一套可继续编辑的护栏。立柱间距大约 1.2 米，随路径转向并正确落在坡面上，扶手沿弯道连续。两种模块有稳定、可调的分布变化，并能调整整体高度和间距。保留已有步道、材质、相机及其他资产。完成后检查转弯处和坡度变化处，给出能看清整体与局部连接的视觉结果。

Between turns, the tester uses the fixture's recorded `move_curve()` operation:
existing guide points 29–36 move by local `(0, 0, -0.5)`, and the existing opening
marker becomes the node selection. Preserve the original network and record this
tester change separately from model calls. Do not help repair the generated asset.

### Frozen second prompt

> 我刚调整了右侧这段曲线。请在现有护栏上继续修改：把立柱间距改为 0.8 米，整体加高 20%，并在我选中的位置附近留出约 2 米的通行开口。保留现有模块分布规则和其他场景内容，不要整套删除重建。再检查新开口和改动后的转弯连接。

### Evidence and outcome

Retain both complete two-turn records and failures, with the frozen versions,
fixture copies, original prompts, actual model/effort, native history, receipts,
HIP snapshots, overall/local images and preservation/continuity checks.
`scripts/tc1_evidence.py` counts external MCP calls, inspect views and metadata
requests, and extracts existing receipt timing; internal polls are not calls.
Review API guessing, waste and first useful authoring latency against actual items.

All original behavior/quality conditions remain in [tc1-brief.md](tc1-brief.md).
No 20% improvement, zero-guess benefit or successful continuation is claimed yet.
Insufficient benefit may justify bounded ranking/result/description corrections.
Address/state/side-effect defects block delivery of the affected capability; they
do not authorize automatically rebuilding the runtime or freezing every tool.

TC-2's later two-turn shutter task must have its own record. It cannot close TC1-A1.

## TC2-A1 — two-turn editable horizontal blinds

- **Owner:** Codex. **Status:** authorized and queued after TC1-A1; not run or passed
  yet. The isolated TC2 account was verified through native account/read. Existing
  test authentication stays in place; production credentials are not copied.
- **Candidate:** the final reviewed TC-2 head, frozen with the immutable
  `tc2-a1-candidate` tag. Its exact SHA is registered in the TC-2 PR before technical
  merge. Keep it separate from the frozen TC1-A1 candidate.
- **Fixture:** `.runtime/reviews/tc2/fixture/source.hip`, made by
  `scripts/tc2_fixture.py`. Existing window opening/frame/blade module/camera/unrelated
  asset; parent translation/rotation/nonuniform scale; no completed blind network.
  Initial `source-report.json` and fresh `fresh-load-report.json` record 17 nodes and
  zero cook errors. Run on a copy; preserve source and every failed model output.
- **Conditions:** H22.0.368 and the existing native Codex 0.153.4, version rechecked
  before admission. `gpt-6-astra` / `high` is advertised by the authenticated native
  model catalog. Record actual advertised and native confirmed
  settings; if unavailable, record the condition before choosing a supported setting.
  This is one two-turn task, not a TC-1 baseline comparison or a percentage claim.
- **Recovery:** first run TC1-A1 when the authorized login opportunity becomes
  available. The local `Start TC2 Acceptance.vbs` in `.runtime/reviews/tc2` uses the
  ordinary launcher with `acceptance/state` and `acceptance/cache`; complete its
  official login without copying/resetting normal state. Use production Studio and a
  fresh native Thread on a copied fixture inside the TC-2 review directory.
- **Isolation:** the model sees the normal scene and the prompts below. Do not give
  it this brief, expected tool sequences, probe results or a prebuilt solution.
  Technical driver runs are separate and cannot count as natural-language evidence.

### First prompt

> 利用场景里已有的窗洞、窗框和叶片模块，制作一套可继续编辑的横向百叶。叶片数量、厚度和开合角度需要可调，整体放在现有窗洞内。保留其他场景内容，检查叶片间距和侧面连接，并给出能看清正面及侧面结构的图像。

Keep the complete turn, receipts, front/side images and a round-one HIP snapshot.
Through a separate recorded tester operation call `move_parent()` from
`scripts/tc2_fixture.py`: change the existing window parent's translation by
`(0.8, 0.2, -0.6)`, add 22 degrees to its Y rotation, and move the viewport to the
unrelated region around `(100, 1, 100)`. The helper changes no blind network.
Retain the before/after transform and viewport facts; exclude this tester operation
from model call counts. Stay in the same Thread/HIP and submit the second prompt.

### Second prompt

> 我刚调整了这组窗户的位置和朝向。请继续修改原来的百叶，改为十二片，并让开合角度在第 24 到 72 帧从 15 度变到 65 度。保留我的场景调整，不要整套重建。检查结束姿态是否碰到窗框，补一张第 72 帧能看清侧面关系的图像。

### Evidence and completion

Save both complete native turns, original receipts/images, round-one and round-two
HIP snapshots and tester edits. Fresh-load/cook the resulting network; check actual
controls/geometry, 12 blades, angle values at 24/72, frame clearance at the requested
end posture, original network continuity, retained parent edit and unrelated assets.
Sampled endpoint evidence is not verification of the entire animation interval.

Use `scripts/tc1_evidence.py THREAD_JSON RECEIPT_DB --stage tc2` on the isolated
exports. It counts external execute/inspect/lookup/capture calls, extracts each
original `observe_after` count once and records target/frame/restoration facts.
Manually classify post-create extra inspections, viewport-preparation-only execution,
API guesses/repeats, partial correction method, continuity and visual quality.
Do not inject controlled faults into this task or omit failures from the record.

Technical validation is recorded in [tc2-validation.md](tc2-validation.md). The
conditional technical merge does not close this model-effect acceptance. A completed
TC-2 task also does not close TC1-A1 or establish a reduction in total model calls.

## TC3-A1 — two-turn reusable procedural walkway asset

- **Owner:** Codex. **Status:** explicitly authorized, queued after the frozen
  TC1-A1 and TC2-A1 runs. Not run or passed yet.
- **Frozen candidate:** immutable tag `tc3-a1-candidate`, commit
  `c03317d6db36acb1533c042f9c094c3322ed3a65`. Never move this tag or substitute the
  later main/feature head. Consent and conversation PRs remain separate changes.
- **Fixture:** `.runtime/reviews/tc3/fixture/source.hip`, generated by
  `scripts/tc3_fixture.py`; 18 nodes, zero fresh-load/cook errors, no target
  generator/HDA. Two different elevated guides, existing modules, exclusions and
  an unrelated asset remain the authoring input. Preserve the original and each
  failed output; operate only on the new run copy.
- **Conditions:** Houdini 22.0.368, native Codex 0.153.4 and authenticated catalog
  `gpt-6-astra` / `high`. Preserve actual native turn_context model/effort records.
  The explicitly authorized isolated TC2 state/CODEX_HOME is used in place with a
  fresh workspace, session and native Thread. HIP/HDA exports and reports belong
  under `tc3/model/<run_id>`; receipts/native history remain under that workspace
  in `tc2/acceptance/state`. No copied credentials or production state.
- **Prompts and requirements:** the complete two-turn task in
  [TC-3 approval section J](tc3-brief.md#j-real-houdini-acceptance), verbatim. Only
  the explicit new test output directory is appended. The tester's `move_inputs()`
  edits the original guide and exclusion between turns; it does not repair the
  generated network. No expected nodes, steps, call counts or solution is supplied.
- **Evidence:** original native turns, runtime receipts/step details, every failure,
  HIP/HDA versions and meaningful images/geometry checks. Fresh-load/cook and a
  clean second-curve HDA instance must verify real outputs, UVs, stable piece IDs,
  independent controls, updated-input behavior and preservation. Technical paving
  fixtures and green CI cannot substitute for this artifact-quality evidence.
