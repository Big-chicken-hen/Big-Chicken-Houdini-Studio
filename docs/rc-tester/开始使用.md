> 历史 f0fce6e 四文件交接说明，原包保留供回溯。当前交付为 `0.1.0-rc.1-4e9db5dec1d7` 五文件 ZIP，使用包内 TXT 说明；日常入口统一为 `Studio.exe`（开始菜单指向它），正常启动沿用 Houdini 用户配置。身份及校验见 [交付记录](../rc-trial-validation.md)。

**Big-Chicken Studio 测试版 · 0.1.0-rc.1-f0fce6e5fd2b**

本目录只有四个文件：`Installer.exe`、`Installer.exe.sha256`、`开始使用.md`、`测试结果.md`。双击 `Installer.exe` 安装；之后日常只从开始菜单的 **Big-Chicken Houdini Studio** 入口打开。无需开发目录、Python、Node、Git 或命令行，也不必计算 hash；`.sha256` 仅供需要时核对传输损坏。本测试包未数字签名，Windows 可能显示未知发布者或信誉提示；若无法安装，请记录 BLOCKED 和提示截图。

使用 Windows 11 x64、正常可用的 Houdini 与许可证。已验证组合为 **Houdini FX 22.0.368**；其他允许的 22 系列 build，以及 Core、Indie、Education，属于 **Untested**，需明确确认后试用；后续 22.x minor 需手动选择。未知事实保持 Unknown。Untested 测试不能替代已验证组合下的完整非管理员标准用户验收。

**Clash 是本次试用的前提。**保持你自己的有效配置，端口可以不同；不用复制开发者的 Clash、环境变量或配置，也不做关闭 Clash 的测试。使用你自己的 ChatGPT 官方账号，在启动器中完成浏览器登录；不要复制他人的认证文件。

安装后，在启动器右上菜单 → 设置 → 诊断中核对 build ID 为 `0.1.0-rc.1-f0fce6e5fd2b`，Codex 默认来源为 **Bundled**，版本为 **0.153.4**。详情中的 Houdini“已选择”和连接后的“正在运行”应与本次实际安装一致。点击“空场景”进入 Houdini 的 Studio Panel，然后按《测试结果.md》完成十项记录。

HIP 用 Save As 保存到你选择的位置，并保留 Houdini 实际采用的文件后缀。默认用户数据在 `%LOCALAPPDATA%\BigChickenStudio\state`，缓存在 `%LOCALAPPDATA%\BigChickenStudio\cache`，实际路径以本地详情为准。升级前正常关闭相关会话；卸载只移除程序与入口，保留认证、历史、场景和用户数据。缓存可能含未保存场景的输出，不要手动清空或移动数据目录。

反馈时填写结果表，附“发生了什么”和相关截图。诊断位置：启动器右上菜单 → 设置 → 诊断 → **导出诊断…**，ZIP 保存到你选择的位置。默认 ZIP 不含聊天正文、凭证、图片或作品；无需阅读源码或分析日志。升级包尚未提供或网络、许可不满足时，填写 BLOCKED，不猜测原因。
