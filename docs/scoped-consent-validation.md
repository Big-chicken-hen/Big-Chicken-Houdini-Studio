# Scoped consent usability

The 2026-09-08 Pro approval authorizes this independent correction. Scope: an MCP
card opens the existing conversation consent explanation, bounded non-reuse reasons,
and exact unique matching among unconsumed in-flight calls. Native 0.153.4 prompt
policy, metadata allowlist and account/Thread/runtime/epoch revocation remain intact.

Opening consent sends no response. A request already pending before a grant remains
manual, including a duplicate delivery of that same pending request. Single approval
still permits only that native request. Response uncertainty disables delegation and
does not cause a resend. Consent does not classify arbitrary Python or authorize
destructive effects without the specific confirmation described by scene instructions.

Validation: 15 focused consent tests and 9 native Qt request-control tests passed,
plus Ruff. Cases cover sequential and distinct concurrent calls, duplicate candidates,
redacted payloads, exact full staged arguments, old requests, revocation, scope changes,
process exit and uncertain response. A 390-pixel-wide offscreen card was rendered and
visually checked using the existing preview font setup; no layout or icon system was
replaced. Raw preview stays in this worktree's `.runtime/reviews/scoped-consent/`.

The installed native executable still reports 0.153.4. There is no new authenticated
user trace proving that this matching condition explains every repeated approval
reported in real work. That causal claim and real user acceptance remain unverified.
The final candidate's CI is checked independently before merge.

## User-reported issue for Pro review

[PANEL-1](acceptance-issues.md): old messages appear with the current Codex message,
and some messages appear interrupted/truncated. This is the user's report, not a
confirmed sending or rendering diagnosis. At the user's explicit instruction it is
recorded only: **not reproduced, not repaired**. Approval changes do not close it.
