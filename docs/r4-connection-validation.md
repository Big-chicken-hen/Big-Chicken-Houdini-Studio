# R4-CONN-1：发送恢复修正与连接取证

2026-09-16。范围来自用户提供的完整 [Pro 审查](r4-connection-review.md)。

**2026-09-17 交付更新：**用户随后明确授权更新安装包、暂不实施 Mermaid。
当前交付已替换为包含本轮修正的 `0.1.0-rc.1-b6bed7cdac13`，详见
[交付记录](r4-package-refresh-20260917.md)。下文 `145c434` 的独立包及
`e6beb5a` 的原 ZIP 保留历史身份，不代表它们仍在本机。新包的真实安装验收仍待完成。

**状态：完整 CI 与离屏渲染通过，用户确认本轮 E 盘日常环境复测正常。独立验收包曾构建并校验，现已按用户要求删除；历史结果保留。现场 10048 因果归属和发行安装流程仍未完成验证。**
此记录不宣称 Studio 导致或已经解决系统端口耗尽，不重新打开 R4-HELP-1。

## 7d94688 的 CI 失败与测试适配

[CI 35046247959](https://github.com/Big-chicken-hen/Big-Chicken-Houdini-Studio/actions/runs/35046247959)
已结束：四个 Windows/Ubuntu、Python 3.10/3.13 backend 作业通过，native-ui
执行 140 项测试时有一个 error；后续 Panel、Launcher 和 DPI 渲染步骤跳过，
不能计作通过。本机保存的原始失败日志位于同一 maintenance 目录下的
`ci-35046247959-native-ui-failure.log`。

`test_deleted_thread_rejects_its_late_turn_acknowledgement` 在读取 `/turn`
回调时抛出 `StopIteration`。旧测试拦截 `panel.call`，而原消息终态恢复路径
通过 `api.call` 发送，因此尚未进入删除保护断言。这是测试拦截位置不匹配，
不是“删除保护已失败”的证据；也不改变此前 42 项定向检查通过的历史范围。

本次仅修改测试与证据：该发送测试改在 Api 层捕获回调，保留生产
`current_submission()` 检查真实运行；根据捕获请求构造完整、身份匹配的
`submission` 接纳回应。正向对照确认未删除时可以结算，原测试确认删除后
相同协议的迟到回应不能恢复 Thread、原提交、等待项或已删除草稿。未跳过、
弱化或容忍缺失请求，未修改任何产品代码。

本地再次执行完整 UI 套件：**141 项全部通过，Ruff 通过**。日志为
`local-full-ui-after-test-adaptation.log`。修正提交的完整 CI（包括此前跳过的
渲染步骤）仍需以 GitHub 的该提交作业结果为准；通过后才能固定独立验收包。
新包需单独标识，原 `e6beb5a` 交付 ZIP 及其历史结果继续保留。

## 固定候选 145c434 与独立验收包

测试适配提交为 `145c434d0b9c02c53f64c9386dc14603f0a006d6`。
[CI 35047403865](https://github.com/Big-chicken-hen/Big-Chicken-Houdini-Studio/actions/runs/35047403865)
**五个作业全部通过**：四个 backend 组合与 native-ui。完整 UI 为 141 项；
基础 Panel、release conversation/progress、Panel 150%/200%、Launcher
100%/150% 渲染步骤均成功，未跳过。对应 artifacts 已在 E 盘保存，并核看
Launcher Ready 图及包内窄 Panel Stop 图；不等同真实 Houdini GUI 验收。

只有上述 CI 完成后才运行离线构建，源码和 builder 均绑定到该提交。
当时的新包为 **`0.1.0-rc.1-145c434d0b9c`**，曾保存在
`E:\Big-Chicken-Houdini-Studio\.runtime\conn1-build-145c434`，作为独立验收候选。
该目录现已按用户要求删除；以下清单和哈希记录历史构建，不代表产物仍在本机。
`发布包` 中原外测 ZIP 的大小和 SHA-256 再次核对未变，未替换或删除。

| 新包检查 | 结果 |
| --- | --- |
| 清单 | 245/245 文件 SHA-256 匹配，无额外载荷文件 |
| 随包运行时 | Python 3.13.15，实际包内 Panel/shared/diagnostics/PySide6 模块 |
| 包内隔离回归 | 24 项通过；会话删除正反对照、连接恢复、HTTP 生命周期、诊断 |
| 包内离屏渲染 | 12 张 Panel 截图生成，确认模块来自新载荷 |
| 独立 ZIP | 五文件、CRC 读回、内层安装器哈希与四份文档内容匹配 |
| 真实 Studio.exe/Houdini、模型操作、系统安装器 | 未运行，不计 PASS |

包内回归第一次尝试记录了一个 `QtTest` 导入错误：发行包按既定规则不含该
测试模块。原日志和 JSON 保存在 `packaged-python-initial-no-qttest.*`，
没有覆盖。之后仅从同一个哈希锁定的 PySide6 6.8.3 wheel 提取 QtTest.pyd
和 Qt6Test.dll 到本机独立测试支持目录，由测试进程显式加载；未加入或修改
安装载荷、Houdini 或任何全局配置。24 项通过的结论包含这项测试依赖边界。
包内离屏渲染本身不需要额外 QtTest。测试结束后重新核对完整载荷清单。

| 独立验收产物 | Bytes | SHA-256 |
| --- | ---: | --- |
| Installer.exe | 216143016 | `e4a66f8da83863185f6e5d0f9453bf86a7c018794793cb2f42d5bc83a297d8ae` |
| Connection-Acceptance.zip | 215691705 | `d23c7a6ad324dbbbd149647331186747f49621142583f0e34438a1ccae15e837` |

外层 README 明确标识本轮为 R4-CONN-1 独立验收，不继承旧包的实机结论，
发送/补充、断连恢复、慢历史、双 Panel、Stop、长时使用及标准用户流程均
保持未预填状态。完整包名为
`Big-Chicken-Studio-0.1.0-rc.1-145c434d0b9c-Connection-Acceptance.zip`。
[机器可读摘要](evidence/r4-connection/candidate-145c434.json) 绑定上述身份。

## Owner 日常真实环境复测（2026-09-16）

用户要求改用日常真实环境复现原 WinError 10048，随后明确反馈：
**“没问题了！！提交吧！！”**。本轮记为用户确认日常环境使用正常，保留该反馈，
不要求为提交记录重复已经进行的操作。

本机同时观察到新会话 `1ef4c2216db64899b0c8e2ce7fc850c4`：Houdini PID
21088 于 10:50:40（UTC+08:00）启动，实际 EXE 来自 Houdini 22.0.368。
诊断身份记录指向 E 盘日常项目的 `src/studio/ui/panel.py`、`shared.py`
及 `http_diagnostics.py`，与本次日常入口范围一致。观察时工作树为 `ef17758`，
生产文件相对固定源码候选 `145c434` 无差异。该会话不冒充独立发行载荷测试。

限时诊断保留了一段 **30.075 秒**的汇总：80 次请求开始、79 次完成、0 次
失败、窗口结束时 1 次在途。其中包括状态/事件/账号、历史及附件请求，
该窗口没有 `/turn` 或 `/turn/steer` 条目。用户后续反馈与这段遥测分别记录；
没有以短窗口零失败替代发送/补充、Stop、断连恢复或长时验证，也没有补写
用户未逐项说明的结果。诊断已按原定 10:52:35 到期，日志保留在本机缓存中。

标准用户安装、独立新包真实 GUI 及现场因果归属继续保持各自待验收状态；
PR #14 保持 Draft，R4-CONN-1 的剩余项保持开放，原外测 ZIP 未替换。
[本轮反馈与观察摘要](evidence/r4-connection/owner-20260916.json) 不含聊天正文、
账号、token 或原场景内容。

## 用户要求的本机清理（2026-09-16）

用户明确不需要独立验收包，并要求控制 `.runtime` 的无用占用。已删除
`conn1-build-145c434` 中的 ZIP、安装器、校验文件和解包副本；另清除旧
`worktrees/readiness` 的闲置测试 venv 和 pip 缓存，以及本轮临时 QtTest
依赖和空构建目录。删除前核对了 E 盘绝对路径、目录包含关系、无重解析点
以及相关进程未使用这些目标。没有删除工作树源码或已有未提交改动。

实际删除 1,300,898,726 字节（约 1.21 GiB）；`.runtime` 的文件总大小由
2,965,010,996 字节降至 1,667,327,586 字节（约 2.76 → 1.55 GiB）。
扫描期间正常运行的数据仍可增长，前后差值不用于声称所有写入均已停止。
当前 Houdini 21088、日常 Python/Codex 依赖、账号、原生历史、工作空间、
场景输出、必要诊断和原 `e6beb5a` ZIP 均保留。构建成功与失败的历史证据
没有因此改写；清理没有修改产品代码，也没有新增自动删除用户数据的机制。

后续测试和构建结束后清理本轮可丢弃产物，仅保留必要证据；不再默认另建
独立验收包。验收记录提交 `72f9b11` 的
[CI 35050005941](https://github.com/Big-chicken-hen/Big-Chicken-Houdini-Studio/actions/runs/35050005941)
五个作业全部通过。本次清理记录提交需独立查看其 CI，不能套用该结果。

## 基线与证据边界

本轮修改前，E 盘工作树为 `2ec79c2`，`ui/shared.py`、`ui/panel.py`、
`http.py`、`bridge.py` 相对 RC 源码 `e6beb5a` 无差异。已有的
`docs/authoring-results.md` 未提交修改由此前工作产生，本轮未编辑或纳入。
这项磁盘核对不能证明故障发生时 Houdini 已加载模块的身份。

Windows System/Tcpip 原始事件重新读取并保存在本机私有目录：
`E:\Big-Chicken-Houdini-Studio\.runtime\maintenance\connection-10048-20260916\system-tcpip-events.json`。
其中包含原始 XML、消息、事件记录号和采集时间，不随 Git 上传。

| 时间（UTC+08:00） | 事件 | 记录号 |
| --- | --- | --- |
| 09:08:06.0386123 | 4231 | 131643 |
| 09:08:12.7725606 | 4227 | 131644 |
| 09:12:25.6806947 | 4227 | 131658 |

这些事件支持该时间段系统出现临时端口分配/传出连接问题。尚无截图报错
对应的请求 ID、路由、调用栈及准确时间，不能直接完成应用层因果归属。
此前约 21,549 条 TIME_WAIT、随后 IPv4 15,896 条（其中纯回环 1,437 条）
为当时不同采样范围的汇总；完整原始连接快照未作为文件留存，本轮没有用
新的快照冒充事故时的快照。事后的活动 PID 数量不能给已关闭连接归责。

## 已实施的小范围修正

1. `_HttpJob` 在调用 `HTTPConnection.request()` **之前**标记请求可能写出。
   socket 创建、连接或连接后取消发生在该标记前，才可归为 `not_submitted`。
   请求可能部分写出、响应头丢失、响应截断等仍为 `unknown`，不自动重发。
   `status is None` 不作为未发送证据。
2. 消息 POST 的终态按原 Api、原消息快照、native connection/account 身份
   处理，不再因一个后台读取推进 UI 连接代次而被丢弃。普通历史/事件回调
   仍使用原来的代次隔离。账号或 native connection 改变、Api 替换、Panel
   关闭及已删除对话不能借此接受旧结果。
3. 明确未发送时解除当前提交的不确定状态，恢复空输入区；已有新草稿或
   IME 组合输入时保留原快照供原有入口查看，不覆盖新草稿。不自动再次发送。
   内存草稿/快照不被描述成跨重启持久化。
4. `/state` 失败后，基础轮询按 1.7、3.4、6.8、13.6、20 秒退避，20 秒封顶。
   在故障模式中只保留去重的 `/state` 探测，暂停常规账号和历史读取；显式
   刷新连接可立即尝试一次。有效状态恢复后恢复原 850 毫秒间隔，合并读取
   events/account/history。没有给 turn/steer 增加自动重试队列。
5. 传输错误详情带时间、PID、Api/请求身份、归一化路由、本机目标端点、
   阶段、异常类型和 errno/winerror。关联消息只用摘要；堆栈仅保留函数、
   行号和 Studio 内的相对文件名，外部文件路径替换为 `<external>`。
   不记录请求正文、认证头、图片/附件内容、环境变量或历史。

仍使用现有 Python HTTP IO、独立 Stop HTTP 线程和标准库解析器，未启用
连接池、HTTP/1.1、Qt Network 回退、全局 TCP 设置或代理更改。

## 默认关闭的限时诊断

`http_diagnostics.py` 提供 Panel 本地取证。没有启用标记时不会建立日志文件。
启用仅对该缓存根中新建的 Panel Api 生效：

- 标记：`<BCS_CACHE_ROOT>/diagnostics/http-trace.enabled`。
- 有效期：从标记的修改时间起 15 分钟；过期标记不会在以后启动时重新启用。
- 输出：同目录 `panel-http-<PID>.jsonl` 及一个轮换文件。
- 容量：每个进程/缓存根至多两个 256 KiB 文件；不同进程的文件分别保留。
- 异常即时记录安全字段；每 30 秒在请求结束时输出路由级请求、完成、失败、
  取消、在途和峰值计数。计数包含排队等待的 HTTP 工作请求，不等于 TCP
  socket 数量或 TIME_WAIT 数量；它只覆盖 Panel → Bridge，不涵盖整台系统。
- 开头记录采集版本及模块位置。磁盘路径本身不是已加载字节的证明；后续
  验收还应记录候选提交和实际启动入口。
- 未启用任何真实用户会话的采集。本轮仅在隔离测试目录验证该机制。
- 原有诊断 ZIP 白名单没有扩大，本地 trace 不会被自动打包或上传。

取证前先保存未确认原文、正在编辑的新草稿和附件引用，避免通过关闭 Panel
或 Houdini 丢掉尚未到达 Bridge 的内存内容。采集可在下一次正常新开会话前
准备；不需要为获取路径反复打断当前场景。

## 定向验证

Windows 本地 Python 3.10.11 / PySide6 6.8.3 / Qt 6.8.3；Qt 使用 offscreen。
测试使用 E 盘隔离状态和 loopback HTTP 接收端，不运行模型或 Houdini，不
制造真实端口耗尽，不使用真实场景、账号和认证数据。

| 检查 | 数量 | 结果及边界 |
| --- | ---: | --- |
| 新连接故障及 Composer 组合测试 | 8 | 通过；含普通发送、补充、后台代次竞态、写出阶段未知、迟到接纳、账号切换、退避和诊断脱敏 |
| 诊断默认关闭/过期/轮换 | 2 | 通过；路径归一化及日志容量检查 |
| 既有 steer 与账号恢复 | 15 | 通过；保留原意图、Turn、草稿和许可边界 |
| 既有真实 loopback HTTP 生命周期 | 7 | 通过；含响应丢失/截断不重放、关闭取消、去重及普通工作池占满时 Stop |
| 既有历史、历史竞态及 NET 历史恢复 | 10 | 通过；保留内容、分页、代次和有界补读 |
| Ruff `src scripts tests` | 1 次 | 通过 |

最初在未修改产品源码时运行前三个新故障测试，三个均失败：普通发送及补充
快照为 `unknown`，后台断连先发生时原发送快照仍是 `pending`。HTTP 写入方法
未调用、隔离接收端未收到 POST。修正后验证原文/附件保留、恢复后入口可用，
只有再次明确点击才发出一条新请求。该旧失败/新通过结果证明恢复修正，不证明
现场 10048 的来源。

## 仍需完成的验收与交付

- 下一次现场错误：关联同一请求的阶段/路由/PID、系统事件、IPv4/IPv6
  活动与 BOUND 等连接状态，以及相关进程父子关系；保持 Clash 正常使用。
  先做一次有界采集，若仍不足再判断是否需要更深入的网络事件跟踪。
- 后续重新批准构建的交付包：断连恢复、慢历史、双 Panel、关闭重开、普通任务与补充、
  Stop，以及覆盖此前故障使用时长的真实 GUI 检查；记录实际时长和连接趋势。
  本次离线回归不能替代这些检查。
- 标准用户安装、升级、卸载重装及完整创作流程仍独立 pending。

2026-09-16 时原 `发布包/Big-Chicken-Studio-0.1.0-rc.1-e6beb5af2de4-Tester.zip` 未改动，
**不包含本轮源码修正**；它于次日按新的交付授权被替换，见本文顶部更新。
本轮独立候选已按用户要求删除，仅保留历史验证记录；
不自动重建，也不将它标记为通过真实新包验收的 RC。
保持 R4-CONN-1 开放，最终发布条件暂不能按上一轮条件式签字直接判定满足。
